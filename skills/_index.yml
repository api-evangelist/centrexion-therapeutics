generated: '2026-08-09'
method: generated
source: openapi/centrexion-therapeutics-content-openapi.yml
note: >-
  Agent Skills generated by the API Evangelist enrichment pipeline against the derived Centrexion
  Therapeutics content OpenAPI. Centrexion publishes no AGENTS.md, no skill files and no agent
  guidance of any kind, so nothing here is provider-authored — these are our operating instructions
  for a surface we catalogued. Every operationId referenced below exists verbatim in
  openapi/centrexion-therapeutics-content-openapi.yml. All three skills are read-only, because the
  entire catalogued surface is read-only.
skills:
- file: centrexion-therapeutics-read-corporate-content.md
  name: Read Centrexion's corporate content
  description: >-
    Retrieve the company's public pages — pipeline, leadership, contact, policies — and the rendered
    HTML that is the only machine-reachable form of its drug-programme and staff content.
  api: openapi/centrexion-therapeutics-content-openapi.yml
  operations: [listPages, getPage, search, getApiIndex]
- file: centrexion-therapeutics-harvest-media-library.md
  name: Harvest the media library safely
  description: >-
    Enumerate media attachments while working around the X-WP-Total counting defect on this
    deployment, which over-reports the collection and will trap a naive pager in empty pages.
  api: openapi/centrexion-therapeutics-content-openapi.yml
  operations: [listMedia, getMediaItem]
- file: centrexion-therapeutics-discover-surface.md
  name: Discover and re-validate the API surface
  description: >-
    Read the route index, registered types, taxonomies and statuses to establish what exists before
    calling anything, and to detect drift on a surface that carries no compatibility promise.
  api: openapi/centrexion-therapeutics-content-openapi.yml
  operations: [getApiIndex, listPostTypes, getPostType, listTaxonomies, getTaxonomy, listStatuses, listCategories, listTags]
excluded:
- operations: [listUsers]
  reason: >-
    /wp/v2/users returns records about identifiable people (display name, slug, biography, avatar
    URLs) to an anonymous caller. Packaging a working enumeration of it as an agent skill would ship
    a people-harvesting tool, so it is deliberately omitted. The endpoint's existence and shape are
    documented in the OpenAPI and in conventions/centrexion-therapeutics-conventions.yml under
    personal_data; no individual record is reproduced anywhere in this repo. This exclusion is
    recorded rather than silent so the omission reads as deliberate.
- operations: [listComments, getComment]
  reason: >-
    Comment objects carry commenter-supplied personal data (author_name, author_url,
    author_avatar_urls). Only a boilerplate WordPress sample comment exists today, but the shape is
    personal data regardless and there is no consumer value in walking it.
- operations: [listReusableBlocks, listDynamicTemplates, listCustomCards, listPostSeries, listNavigation, getNavigation, getOembed, listPosts, getPost]
  reason: >-
    Empty, boilerplate or presentational on this deployment — the post collection holds only the
    stock "Hello world!" placeholder, the theme custom types and reusable blocks report zero items,
    and navigation and oEmbed return site chrome. No skill would have anything to operate on.
